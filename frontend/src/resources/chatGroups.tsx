//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const chatGroupsTitle = () => {
  const record = useRecordContext();
  return <span>chatGroups {record ? `"${record.groupName}"` : ''}</span>;
};

export const chatGroupsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="groupName" />
      <TextField source="groupDescription" />
      <DateField source="creationDate" />
      <ReferenceField source="listOfMemberIds" reference="users" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const chatGroupsEdit = () => (
  <Edit title={<chatGroupsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="groupName" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="groupDescription" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="creationDate" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="listOfMemberIds" reference="users" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const chatGroupsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="groupName" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="groupDescription" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="creationDate" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="listOfMemberIds" reference="users" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  <ReferenceInput
    source="listOfMemberIds"
    label="listOfMemberIds"
    reference="users"
    alwaysOn
  />,
];
